// Replace the values below with your Firebase project configuration.
// You can find them in your Firebase console under Project settings > General.
window.firebaseConfig = {
  apiKey: 'YOUR_FIREBASE_API_KEY',
  authDomain: 'your-project-id.firebaseapp.com',
  projectId: 'your-project-id',
  appId: 'YOUR_FIREBASE_APP_ID',
};

// List the email addresses that should have access to the coaching log.
window.adminEmails = ['coach@example.com'];
